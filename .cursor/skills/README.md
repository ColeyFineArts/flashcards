# Cursor skills (this repo)

| Skill | Invoke | Cloud note |
|-------|--------|------------|
| deepen-plan | `/deepen-plan` | No `plan_panel` without Topics |
| diagnose | `/diagnose` | Scratch under `/home/ubuntu/.cursor/scratch/` |
| implement-plan | `/implement-plan` | Drive verify after every `create_file` |
| improve-agents-md | `/improve-agents-md` | Mine via `cursor-cloud` transcripts |

Operating card: repo root [`AGENTS.md`](../../AGENTS.md).
